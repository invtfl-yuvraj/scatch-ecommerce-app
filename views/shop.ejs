<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop | E-commerce</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-gray-900">

    <!-- Navigation -->
    <header class="w-full flex justify-between items-center px-20 py-4 border-b">
        <h1 class="text-2xl font-bold">Shop.</h1>
        <nav class="flex gap-6 text-md ">
            <a href="#" class="transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">Home</a>
            <a href="#" class="transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">Products</a>
            <a href="#" class="transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">Cart</a>
            <a href="#" class="transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">My account</a>
        </nav>
    </header>

    <!-- Main Content -->
    <div class="flex px-20 py-10 h-[90vh] w-full">

        <!-- Sidebar -->
        <aside class="w-1/5 h-full">
            <div class="w-full h-full flex flex-col justify-between pr-10 py-10">
                <div class="flex flex-col gap-4">
                    <div class="mb-6">
                        <label class="text-lg font-semibold">Sort by :</label>
                        <select class="border p-2 w-full mt-2">
                            <option>Popular</option>
                            <option>Price: Low to High</option>
                            <option>Price: High to Low</option>
                        </select>
                    </div>
                    <div>
                        <ul class="mt-3 space-y-2">
                            <li><a href="#" class="text-gray-600 transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">New Collection</a></li>
                            <li><a href="#" class="font-bold">All Products</a></li>
                            <li><a href="#" class="text-gray-600 transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">Discounted Products</a></li>
                        </ul>
                    </div>
                </div>
                <div class="mt-10">
                    <p class="text-lg font-semibold">Filter by :</p>
                    <ul class="mt-3 space-y-2">
                        <li><a href="#" class="text-gray-600 transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">Availabilty</a></li>
                        <li><a href="#" class="text-gray-600 transition-all duration-200 hover:scale-110 hover:text-blue-600 hover:font-semibold">Discounted</a></li>
                    </ul>
                </div>
            </div>
        </aside>

        <!-- Product Grid -->
        <section class="h-full w-4/5 grid grid-cols-4 gap-6">
            <!-- Product Cards -->

            <% products.forEach((product) => { %>
                <div class= "h-full w-full max-h-96 flex flex-col justify-between bg-[<%=product.bgcolor%>] rounded-md">
                    <img src="data:image/jpeg;base64,<%= product.image.toString('base64')%>" alt="Product Image" class="w-full h-full p-4 h-1/2 object-contain rounded-md">
                    
                    <div class="w-full h-24 p-6 flex justify-between items-center rounded-b-md bg-[<%=product.panelcolor%>]">
                        <div class="flex flex-col justify-between">
                            <p class="text-lg font-bold text-[<%=product.textcolor%>]"><%= product.name%></p>
                            <p class="font-bold text-[<%=product.textcolor%>]">₹ <%= product.price%></p>    
                        </div>
                        <button class="h-10 w-10 text-xl text-center bg-white rounded-full shadow hover:bg-gray-200">+</button>
                    </div>
                </div>
            <% })%>
            


        </section>
    </div>

</body>
</html>
